{
  "name": "express_backend",
  "version": "1.0.0",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc && cd ../react_frontend && npm install && npm run build",
    "start": "npm run build && node dist/server.js",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "nodemon dist/server.js",
    "client": "cd client && npm run build && npm start",
    "test": "cd client && npm test",
    "eject": "cd client && npm run eject"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.2",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "pg": "^8.12.0",
    "typescript": "^5.5.4"
  },
  "devDependencies": {
    "@types/node": "^22.5.2"
  },
  "description": ""
}
